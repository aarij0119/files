<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Files</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 px-6 py-5">
        <div>
            <form action="/create" method="post">
                <div class="w-full  text-white rounded overflow-hidden">
                    <input type="text" placeholder="Title" name="title"
                        class="w-full bg-zinc-800 border-none focus:outline-none p-2">
                </div>
                <div class="mt-2 text-white rounded overflow-hidden">
                    <textarea placeholder="Desc." name="desc" 
                        class="w-full bg-zinc-800 resize-none px-2 flex items-center pt-3 justify-center focus:outline-none"></textarea>
                </div>
                <div class="mt-2 text-white overflow-hidden">
                    <input type="submit" value="Submit" class="bg-blue-600 px-3 py-1 rounded-md">
                </div>
            </form>
        </div>
        <!--tasks-->
        <div class="mt-12 flex gap-2 flex-wrap">
            <% if(files.length > 0 ) { %>
                <% files.forEach(function (val) { %>
                    <div class="min-w-72 bg-zinc-800 rounded px-4 py-1">
                        <h1 class="text-white text-xl"><%= val %></h1>
                        <div class="flex justify-between">
                            <a href="/file/ <%= val %>" class="text-blue-600 text-[15px]">See more</a>
                            <a href="/edit/ <%= val %>" class="text-zinc-500 text-[15px]">Update file</a>
                        </div>
                     </div>
                <% }) %>
            <%}else{%>
                <h1 class="text-zinc-400 bg-zinc-800 w-56 px-3 py-2 rounded">Not a single tasks yet</h1>
            <%}%>
           

           <!-- <div class="w-72 bg-zinc-700 rounded px-2 py-1">
            <h1 class="text-white text-xl">Some task goes here</h1>
            <p class="text-blue-600 text-[15px]">See more</p>
         </div> -->
        </div>
    </div>
</body>

</html>